{
  "name": "backend-webscraper",
  "version": "1.0.0",
  "description": "overall file mostly for concurrently",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix frontend \" \"npm run dev --prefix backend \" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.6.0",
    "concurrently": "^9.2.0"
  }
}
